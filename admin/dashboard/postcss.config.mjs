/**
 * PostCSS configuration for the Super Admin Dashboard.
 *
 * Uses the @tailwindcss/postcss plugin for Tailwind CSS v4 processing.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
